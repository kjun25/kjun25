# 수학눈(MathNun) 브리핑 — 수익형 웹서비스 검토용

> **이 문서의 용도**: 새 저장소·새 세션에서 "1인 개발자가 수익을 낼 웹서비스"를 설계할 때
> 읽는 배경 자료다. 수학눈의 구현 지침이 아니라 **가진 자산·제약·이미 내린 결론**의 목록이다.
> 수학눈 저장소를 열지 않아도 이 문서만으로 판단이 서도록 썼다.
>
> 숫자는 2026-08-15 기준 실측이다. 추정치는 그렇다고 표시했다.
>
> 사업자등록번호·주소·생년월일은 일부러 뺐다 — 아이디어 판단에 필요 없고, 이 파일은
> 저장소에 커밋될 것이기 때문이다.

---

## 0. 30초 요약

- **만든 사람**: 1인 개발자. 개인사업자 **케이준소프트(KJun Soft)** — 정보통신업 /
  응용 소프트웨어 개발 및 공급업. **2026-01 개업, 현재까지 매출 0원.** 대전 거주,
  30대 후반, 아들을 키운다.
- **만든 것 둘**: `mathnun.com`(초·중 수학 교육, 완성) / `baseball.mathnun.com`(로그라이크
  야구 게임, 개발 중). 둘 다 **서버비 $0**로 돌아간다.
- **트래픽**: 사실상 0. 신규 도메인이고 홍보를 한 적이 없다.
- **광고는 답이 아니라고 이미 결론이 났다**(§7.1에 계산). 애드센스는 심사 중이지만
  기대는 접었다.
- **찾는 것**: 1인이 만들어 실제로 돈이 되는 웹서비스. 위 자산을 재사용할 수 있으면 더 좋다.

---

## 1. 수학눈은 무엇인가

초등·중학생을 위한 **한국어 수학 개념 학습 사이트**. 핵심은 하나다 —
**슬라이더를 당기면 그래프와 도형이 실제로 움직인다.**

교과서·인강이 "y = ax²에서 a가 커지면 폭이 좁아진다"를 글로 설명하는 자리에서,
이 사이트는 학생이 손으로 a를 당겨 3초 만에 알게 한다. 개념 하나에
**설명 → 움직이는 그래프 → 왜 그 공식이 나오는지 애니메이션 → 실생활 예시 → 퀴즈**가 붙는다.

**로그인이 없고 개인정보를 받지 않는다.** 대상이 만 14세 미만이라 법정대리인 동의 의무가
따라오는데, 아무것도 안 받으면 그 조항 자체를 피한다. 진도·즐겨찾기도 이 이유로 걷어냈다.

---

## 2. 지금 있는 것 — 자산 목록

### 2.1 콘텐츠 (전부 직접 제작, 실측)

| 항목 | 수량 |
|---|---|
| 수학 개념 | **48개** — 초1·2 4 / 초3·4 6 / 초5·6 8 / 중1 12 / 중2 10 / 중3 8 |
| 단계별 학습 | 240개 (개념당 5단계) |
| 퀴즈 문항 | **192개** (개념 3 + 실생활 1) × 48 |
| "왜 그럴까?" 공식 유도 장면 | 45개 (2D) + 3개(3D) = **48/48 전수 커버** |
| 실생활 삽화 | **48개** (전부 인라인 SVG, 산 이미지 0개) |
| 조작 가능한 파라미터 | 82개 |
| "이 점을 지나게 만들어봐" 도전 과제 | 13개 개념 |
| 프리렌더된 SEO 페이지 | 51개 |

**교육과정을 따라 배치돼 있다.** 학년 축 하나로만 나뉘고(초1·2 / 초3·4 / 초5·6 / 중1 /
중2 / 중3), 각 개념에 난이도·순서(`order_idx`)가 붙어 있다.

### 2.2 재사용 가능한 기술 자산 ← **여기가 핵심**

새 서비스를 만들 때 **다시 안 만들어도 되는 것들**이다.

- **파라미터로 도형·그래프를 그리는 엔진.**
  `draw<Name>(ctx, s)` 형태의 순수 함수 23개(원·다각형·시계·각도·곱셈판·구슬·자·전개도…)와,
  함수 그래프를 그리는 수식 모듈(`curveMath.js`). 슬러그로 자동 등록되는 레지스트리 구조.
  좌표계는 400×320 고정이고 표시 크기에 맞춰 비율을 지키며 확대한다(`ResizeObserver` +
  `ctx.setTransform`) — **어떤 크기에서도 원이 타원이 되지 않는다.**
  → *문제 생성기·워크시트·교구·다른 과목 시각화에 그대로 쓰인다.*

- **"움직여야 답이 나오는" 애니메이션 4종** — 원주율(원이 한 바퀴 굴러 3.14가 드러남),
  도형의 이동, 최소공배수(톱니 두 개가 다시 맞물리는 순간), 함수 기계.
  경과 시간 기반(`useCanvasClock`)이라 느린 기기에서도 같은 속도다.

- **3D 입체 도형** (three.js + @react-three/fiber) — 구·원기둥·원뿔의 겉넓이·부피를
  **전개도가 펴지는 애니메이션**과 **물을 붓는 장면**으로 보여준다.
  `lazy()`로 분리돼 있어 2D만 보는 사용자는 three.js를 받지 않는다.

- **SVG 삽화 48장** — 전부 인라인이라 네트워크 요청 0, 다크 모드 자동 대응,
  색을 브랜드 팔레트 안에서 바꿀 수 있다.

- **빌드 파이프라인**
  - `SQL 시드 → JSON` 생성기: 콘텐츠를 SQL로 관리하고 빌드 시점에 번들에 굽는다.
    **읽기에 DB도 백엔드도 필요 없다** — 이게 서버비 $0의 이유다.
  - **라우트별 HTML 프리렌더 생성기**: 51개 페이지의 `<title>`·`og:`·canonical을
    각각 다르게 굽고 `sitemap.xml`·`robots.txt`·`ads.txt`·애드센스 확인 메타까지 만든다.
    SPA인데도 카카오·X 링크 미리보기가 페이지마다 다르게 뜬다.
  - 콘텐츠 검증 게이트: 퀴즈 누락·정답 인덱스 범위·장면 커버리지를 빌드마다 검사한다.

- **인쇄용 학습지 생성**(`PrintSheet`) — 화면엔 없고 종이에만 나온다. A4 한 장에
  제목·식·이름칸 → 문제 4개 → 풀이용 빈 모눈. **정답은 일부러 뺀다.**

- **수업 모드**(`?present=1`) — 전자칠판용. 설명·퀴즈·헤더를 걷어내고 그래프와 슬라이더만
  크게 남긴다(캔버스 288 → 464px).

- **상태가 담긴 URL**(`?a=-2.5&b=3`) — 슬라이더 값이 주소에 들어간다.
  선생님이 "이 상태 그대로" 북마크·공유할 수 있다.

- **쓰기 전용 Worker + D1(선택)** — 익명 접속 통계만 받는다. **D1이 없어도 배포가
  정상 동작한다**(501을 조용히 접는 계약).

### 2.3 코드 규모

```
frontend/src   70개 파일 · 12,298줄
  components 21 · pages 4 · hooks 8 · lib 10
진입 번들      gzip 약 298 KB   (3D 청크 gzip 248 KB는 3D 페이지에서만)
```

### 2.4 인프라·도메인

- **Cloudflare Workers** (정적 자산 + `/api/*` Worker). **git push → 자동 배포.**
- **비용 $0.** 백엔드도 PostgreSQL도 배포 경로에 없다.
- 도메인 `mathnun.com` — hosting.kr 등록 + Cloudflare DNS. **만료 2027-01-02.**
- `baseball.mathnun.com` — 같은 도메인의 서브도메인, **별도 저장소**가 서비스한다.
- `kjunsoft.com` — **미등록.** 사업자명 도메인이라 나중에 쓸 여지가 있다.
- Google Search Console 도메인 속성 등록 + sitemap 제출 완료(2026-08).

---

## 3. 또 하나의 자산 — 지옥야구단 (`baseball.mathnun.com`)

텍스트/카드 기반 싱글플레이 **로그라이크 덱빌딩 야구** 웹게임. Slay the Spire의 야구 버전.

- **순수 TypeScript 게임 엔진 4,042줄** (UI와 완전히 분리, 32개 소스 파일).
  타석 확률(Log5 2단계 트리) · 9이닝 시뮬 · 편성/시너지 · 런 상태머신 · 세이브 마이그레이션.
- **모든 무작위가 시드 RNG를 경유한다**(`Math.random()` 금지). 결정론이 보장되므로
  **데일리 챌린지·리플레이·서버 검증·PvP 치팅 방지**가 전부 성립한다. 이건 나중에
  값이 되는 종류의 설계다.
- 밸런스 시뮬레이터(봇 자동 플레이)로 클리어율·축별 승률 편차를 측정하며 튜닝한다.
- 선수 60명 × 3등급, 11개 구단, 감독 5종, 대한민국 지도 시각화.
- 스택: Astro(SSG) + React 아일랜드 + Tailwind v4 + TypeScript. **백엔드 0**(localStorage).
- 아트: 사용자가 직접 만든 로고·히어로 일러스트·감독 초상 5종(캐주얼 톤).

> 참고: 이 저장소는 별도이고, 검토 환경에서 의존성이 설치돼 있지 않아 테스트를
> 재실행하지는 못했다. 저장소 문서 기준으로는 테스트 131개가 통과 상태다.

---

## 4. 운영 현실 (냉정한 숫자)

| 항목 | 상태 |
|---|---|
| 매출 | **0원** (2026-01 개업 이후 계속) |
| 트래픽 | 사실상 0. 신규 도메인, 홍보 이력 없음 |
| 서버 비용 | **$0** |
| 애드센스 | 소유권 확인 완료 · **사이트 심사 중** · 슬롯 ID 없음 → **광고 0개** |
| 결제 수단 | 없음 (PG·구독 붙인 적 없음) |
| 사용자 계정 | 없음 (의도적) |

**가장 부족한 것은 콘텐츠가 아니라 유입이다.** 개념 48개는 이미 충분히 깊다.

---

## 5. 제약 조건 (아이디어를 낼 때 반드시 고려할 것)

1. **1인이다.** 시간이 유일한 자원이고 나눠지지 않는다. 사이트를 여러 개 늘리는 전략은
   이미 기각했다 — 검색은 깊이를 보상하고, 얕은 사이트 10개는 트래픽 0이 10개다.
2. **CS를 감당할 수 없다.** 문의가 하루 10건이면 개발이 멈춘다.
   **소액 B2C 구독(월 1,000~3,000원)은 1인에게 최악**이라는 결론이 이미 났다.
3. **서버비 $0을 유지하고 싶다.** 지금까지 모든 설계가 그 방향이었다.
   (Cloudflare 정적 자산 + 선택적 Worker/D1)
4. **개인정보를 받지 않기로 했다.** 대상이 만 14세 미만이라 로그인은 법정대리인 동의
   의무를 부른다. 유료화하려면 이 결정을 **어디까지 풀지**를 먼저 정해야 한다
   — 선생님(성인) 대상이면 이 문제가 대부분 사라진다.
5. **아동 대상이라 광고 단가가 구조적으로 낮다.** 맞춤 광고를 끄는 것이 옳고,
   그 순간 RPM이 절반 이하가 된다.
6. **화면 규칙(참고)**: 이모지 금지(아이콘은 lucide 또는 인라인 SVG), 두 색 그라디언트
   금지, 어려운 외래어 금지(파라미터·인터랙티브 등), 접근성 대비 4.5:1 실측 준수.
   브랜드를 공유하는 서비스라면 같은 규칙을 따른다.

---

## 6. 이미 검토하고 결론 난 것 — **다시 제안하지 말 것**

### 6.1 광고 수익 — 계산해 보고 기각

수학눈 기준 추정(실제 RPM은 승인 후 데이터로 대체해야 한다):

| | 낙관 | 현실 |
|---|---|---|
| 월 페이지뷰 (1년차) | 30,000 | 5,000 |
| 유효 RPM | ₩800 | ₩300 |
| **월 수익** | **₩24,000** | **₩1,500** |

애드센스 **지급 기준은 $100(약 ₩14만)**. 현실 쪽이면 그 돈이 쌓이는 데 **7~8년**이다.

역으로, **광고로 월 100만 원을 벌려면 월 200만 페이지뷰**(하루 6~7만)가 필요하다.
국내 중견 미디어 규모이고 1인이 닿는 지점이 아니다.

**수학눈이 특히 불리한 구조적 이유 셋** (셋 다 학생에게는 옳은 결정이라 되돌리지 않는다):
- 아동 대상 지정 → 맞춤 광고 꺼짐 → RPM 반토막
- 광고 자리가 페이지 끝 두 곳뿐 → 노출이 페이지뷰의 절반도 안 됨
- 학생은 구매력이 없어 광고주가 단가를 안 씀

→ **광고는 서버비 메우는 용도로만 본다. 수익 모델이 아니다.**

### 6.2 검토한 후보 셋 (아직 실행 전, 하나도 착수 안 함)

| 후보 | 월 100만 원에 필요한 것 | 평가 |
|---|---|---|
| **① 선생님용 문제지 생성** (수학눈 확장) | 월 9,900원 × 100명 | 자산 재사용이 가장 크다. **현 1순위** |
| **② 대전 지역 학원 B2B** (출결·수납·문자) | 월 3~5만 원 × 30곳 | 단가 높고 이탈 적음. 영업 시간이 든다 |
| **③ 지옥야구단 스팀 출시** | 5,000원 × 1,000장 (일회성) | 서버비·CS 0. 완성도에 몇 달 필요 |

①의 차별점 후보: **종이 문제지에 QR을 넣어 그 문항의 움직이는 그래프로 연결.**
문항 수로는 기존 강자(수학대왕·마타수학 등)를 못 이기지만, 종이와 화면을 잇는 건
큰 곳들이 안 하는 일이고 이 사람만 이미 자산을 갖고 있다.

**아직 아무것도 고르지 않았다.** 새 세션에서 ①②③ 밖의 안이 나와도 좋다.

---

## 7. 새 세션에서 판단할 때의 기준

아이디어를 거르는 기준 넷. 넷 다 통과해야 한다.

1. **검색으로 유입되는가** — 마케팅 예산이 0이므로, 사람들이 **이미 검색하는 문제**여야 한다.
2. **문의가 안 생기는가** — 로그인·결제가 복잡할수록 CS가 는다.
3. **단가가 있는가** — 월 1,000원 × 1,000명보다 월 3만 원 × 30곳이 훨씬 쉽다.
4. **한 번 만들고 계속 파는가** — 외주는 현금은 되지만 자산이 안 쌓인다.

그리고 **가장 작은 형태로 2주 안에 파는 데까지** 갈 수 있어야 한다.
처음부터 크게 만들면 팔리는지 모르는 채 반년이 간다.

---

## 8. 아직 안 정해진 것 (열린 질문)

- 유료화 대상을 **선생님(성인)** 으로 잡을 것인가, **학부모** 인가, **학원(B2B)** 인가.
  → 이걸 정해야 로그인·개인정보 정책을 어디까지 풀지가 정해진다.
- 결제 수단(국내 PG / 페이플 / 토스페이먼츠 / 해외 Stripe·Lemon Squeezy)을 무엇으로 할지.
  사업자가 있으므로 **세금계산서 발행이 가능하다** — B2B라면 이게 유리하게 작용한다.
- 수학눈 브랜드 안에서 할 것인가, 별도 서비스·별도 도메인(`kjunsoft.com`?)으로 낼 것인가.
- 지옥야구단을 계속 밀 것인가, 수익화 축을 교육 쪽으로 모을 것인가.
- 외주 수주를 병행할 것인가 (즉시 현금 ↔ 자산이 안 쌓임).

---

## 9. 이 문서를 받은 세션이 할 일

1. §5 제약과 §7 기준을 먼저 읽고, **거기에 안 맞는 아이디어는 처음부터 내지 않는다.**
2. §6에서 이미 기각된 것(광고, 소액 B2C 구독, 사이트 늘리기)을 다시 제안하지 않는다.
3. §2.2의 재사용 가능한 자산을 최대한 활용하는 방향을 우선한다 —
   **"처음부터 만들어야 하는 것"과 "이미 있는 것"을 구분해서 제시할 것.**
4. 아이디어마다 **도달 가능한 숫자**(고객 수 × 단가 = 월 매출)와 **가장 큰 위험 하나**를
   반드시 붙인다. 숫자 없는 아이디어는 판단할 수 없다.
5. 시장 규모·검색량은 **실시간으로 확인할 수 없으면 추정이라고 밝힌다.**
   근거 없는 낙관은 반년을 태운다.
